# 这是一个示例环境变量文件，请复制为.env.development并填入实际的值
# 测试用API地址
VITE_API_TARGET=http://152.53.37.160:25774
# 是否启用 RPC2 WebSocket。默认关闭，后端明确暴露 WS 时再设置为 true。
VITE_RPC2_WS_ENABLED=false
# 可选：RPC2 WebSocket 自定义地址，默认与 /api/rpc2 相同（必须为同域可代理路径）
VITE_RPC2_WS_URL=
